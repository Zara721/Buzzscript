/*
Each buzz quiz has a csv file with the question and option_x field
When you click on a card, it redirects to another html and takes a GET query string to load the correct csv file

*/

document.getElementById("option_a").innerHTML = "Im so bored"
